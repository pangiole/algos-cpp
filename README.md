# algos-cpp
Algorithms and data structures in C++ language.

## build
To build this project, use the following command:
```sh
g++ main.cpp
```

## run
To run this project, once it's built, use the following command:
```sh
./a.out
```

## questions
Ask for the following questions:
- Learning how to code in C and C++ languages.
- In the context of C++ programming, what is the `main` function?
- Tell me more about the return type of the `main` function. Why it's `int`?
- Tell me more about the `a.out` binary file generated by the `g++` command.
- Tell me something about the g++ pipeline (preprocessor, compiler, assembler, linker).